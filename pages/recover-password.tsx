import React, { ReactElement } from "react";
import Layout from "../components/Layout";

interface Props {}

export default function RecoverPassword({}: Props): ReactElement {
  return <Layout title="Login">Recuperar contraseña</Layout>;
}
