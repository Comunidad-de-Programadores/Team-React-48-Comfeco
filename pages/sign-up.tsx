import React, { ReactElement } from "react";
import Layout from "../components/Layout";
interface Props {}

export default function SignUp({}: Props): ReactElement {
  return <Layout title="Registro">Login</Layout>;
}
